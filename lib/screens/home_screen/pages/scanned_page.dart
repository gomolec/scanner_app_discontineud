import 'package:flutter/material.dart';

class ScannedPage extends StatelessWidget {
  const ScannedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ScannedPage");
  }
}
